version: "3.8"
services:

  prometheus:
    image: prom/prometheus:v2.20.1
    volumes:
      - ./prometheus/:/etc/prometheus/
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    ports:
      - 9090:9090